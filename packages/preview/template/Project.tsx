import { FeedBackType } from "../types/Feedback";

interface Props {
    url: string[];
    topText: string;
    bottomText: string;
    feedback?: FeedBackType;
    isTeacher?: boolean;
}

export default function Project(){
    
    return(
        <div>
        
        </div>
    )
}